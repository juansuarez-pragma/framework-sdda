/// DataSource remoto para demo.
abstract class DemoRemoteDataSource {
  // Definir métodos que retornan Models según la API seleccionada.
}

/// Implementación del DataSource remoto.
// @LazySingleton(as: DemoRemoteDataSource)
class DemoRemoteDataSourceImpl implements DemoRemoteDataSource {
  // final ApiClient _apiClient;

  // DemoRemoteDataSourceImpl(this._apiClient);
}
